document.querySelectorAll('.nav-toggle').forEach(button=>button.addEventListener('click',()=>{const menu=document.getElementById(button.getAttribute('aria-controls'));const open=menu?.classList.toggle('is-open');button.setAttribute('aria-expanded',String(Boolean(open)))}));const observer='IntersectionObserver'in window?new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('is-visible')}),{threshold:.12}):null;document.querySelectorAll('.reveal').forEach(el=>observer?observer.observe(el):el.classList.add('is-visible'));document.querySelectorAll('.carousel-prev,.carousel-next').forEach(button=>button.addEventListener('click',()=>button.closest('section')?.querySelector('.sponsor-carousel')?.scrollBy({left:button.classList.contains('carousel-prev')?-320:320,behavior:'smooth'})));